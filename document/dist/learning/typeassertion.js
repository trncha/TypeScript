"use strict";
let username;
username = "top";
console.log(`pattern as => ${username.toUpperCase()}`);
console.log(`pattern <> => ${username.toUpperCase()}`);
// toUpperCase() ใช้งานได้ต่อเมื่อตัวแปรเป็น string
// จะเป็นการระบุชนิดข้อมูลของ username ว่าเป็นชนิดข้อมูล string จริงๆ
